// Generated by CIRCT firtool-1.114.1

// Users can define 'STOP_COND' to add an extra gate to stop conditions.
`ifndef STOP_COND_
  `ifdef STOP_COND
    `define STOP_COND_ (`STOP_COND)
  `else  // STOP_COND
    `define STOP_COND_ 1
  `endif // STOP_COND
`endif // not def STOP_COND_

// Users can define 'ASSERT_VERBOSE_COND' to add an extra gate to assert error printing.
`ifndef ASSERT_VERBOSE_COND_
  `ifdef ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ (`ASSERT_VERBOSE_COND)
  `else  // ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ 1
  `endif // ASSERT_VERBOSE_COND
`endif // not def ASSERT_VERBOSE_COND_
module LSU_Verification_Assert(
  input       reset,
  input [7:0] ar_stall,
              aw_stall,
              w_stall,
              r_stall,
              b_stall,
  input [2:0] io_dmem_arsize,
              io_dmem_awsize,
  input [3:0] io_dmem_wstrb,
  input       _GEN,
              clock,
              _GEN_0,
              _GEN_1,
              _GEN_2,
              _GEN_3,
              io_dmem_arvalid,
              io_dmem_awvalid,
              io_dmem_wvalid,
              _GEN_4
);

  `ifndef SYNTHESIS
    wire _GEN_5 = io_dmem_wvalid & ~reset;
    always @(posedge clock) begin
      if (_GEN & ~reset & ar_stall > 8'hC7) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] LSU.DMEM: arvalid stuck waiting for arready!\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (_GEN_0 & ~reset & aw_stall > 8'hC7) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] LSU.DMEM: awvalid stuck waiting for awready!\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (_GEN_1 & ~reset & w_stall > 8'hC7) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] LSU.DMEM: wvalid stuck waiting for wready!\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (_GEN_2 & ~reset & r_stall > 8'hC7) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] LSU.DMEM: rvalid stuck after AR handshake!\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (_GEN_3 & ~reset & b_stall > 8'hC7) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] LSU.DMEM: bvalid stuck after AW+W handshake!\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (io_dmem_arvalid & ~reset & io_dmem_arsize > 3'h2) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] LSU.DMEM: arsize > 2 invalid for RV32\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (io_dmem_awvalid & ~reset & io_dmem_awsize > 3'h2) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] LSU.DMEM: awsize > 2 invalid for RV32\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (_GEN_5 & io_dmem_wstrb == 4'h0) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] LSU.DMEM: wstrb=0 while wvalid=1\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (_GEN_5 & ~_GEN_4) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] LSU.DMEM: wlast=0 while wvalid=1 (single-beat)\n");
        if (`STOP_COND_)
          $fatal;
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
endmodule

