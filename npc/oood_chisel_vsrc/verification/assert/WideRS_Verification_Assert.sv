// Generated by CIRCT firtool-1.114.1

// Users can define 'STOP_COND' to add an extra gate to stop conditions.
`ifndef STOP_COND_
  `ifdef STOP_COND
    `define STOP_COND_ (`STOP_COND)
  `else  // STOP_COND
    `define STOP_COND_ 1
  `endif // STOP_COND
`endif // not def STOP_COND_

// Users can define 'ASSERT_VERBOSE_COND' to add an extra gate to assert error printing.
`ifndef ASSERT_VERBOSE_COND_
  `ifdef ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ (`ASSERT_VERBOSE_COND)
  `else  // ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ 1
  `endif // ASSERT_VERBOSE_COND
`endif // not def ASSERT_VERBOSE_COND_
module WideRS_Verification_Assert(
  input       reset,
              io_enq_fire_0,
              allocAccept_0,
              io_enq_fire_1,
              allocAccept_1,
              io_enq_fire_2,
              allocAccept_2,
              io_enq_fire_3,
              allocAccept_3,
  input [4:0] io_issue_alu_bits_0_rob_idx,
              io_issue_alu_bits_1_rob_idx,
  input       io_issue_alu_fire_0,
              io_issue_alu_fire_1,
  input [4:0] io_issue_alu_bits_2_rob_idx,
  input       io_issue_alu_fire_2,
  input [4:0] io_issue_alu_bits_3_rob_idx,
  input       io_issue_alu_fire_3,
              io_issue_lsu1_bits_lsu_mem_write,
              io_issue_lsu1_fire,
              io_issue_lsu_fire,
  input [3:0] io_issue_lsu_idx,
              io_issue_lsu1_idx,
  input       io_issue_store_addr_bits_src2_ready,
              io_issue_store_addr_bits_lsu_mem_write,
              io_issue_store_addr_bits_src1_ready,
              io_issue_store_addr_fire,
              clock
);

  `ifndef SYNTHESIS
    always @(posedge clock) begin
      if (~reset & ~(~io_enq_fire_0 | allocAccept_0)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide RS requires pre-checked enqueue credit\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_enq_fire_1 | allocAccept_1)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide RS requires pre-checked enqueue credit\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_enq_fire_2 | allocAccept_2)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide RS requires pre-checked enqueue credit\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_enq_fire_3 | allocAccept_3)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide RS requires pre-checked enqueue credit\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & io_issue_alu_fire_0 & io_issue_alu_fire_1
          & io_issue_alu_bits_0_rob_idx == io_issue_alu_bits_1_rob_idx) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide RS must not issue one instruction to two ALUs\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & io_issue_alu_fire_0 & io_issue_alu_fire_2
          & io_issue_alu_bits_0_rob_idx == io_issue_alu_bits_2_rob_idx) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide RS must not issue one instruction to two ALUs\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & io_issue_alu_fire_0 & io_issue_alu_fire_3
          & io_issue_alu_bits_0_rob_idx == io_issue_alu_bits_3_rob_idx) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide RS must not issue one instruction to two ALUs\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & io_issue_alu_fire_1 & io_issue_alu_fire_2
          & io_issue_alu_bits_1_rob_idx == io_issue_alu_bits_2_rob_idx) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide RS must not issue one instruction to two ALUs\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & io_issue_alu_fire_1 & io_issue_alu_fire_3
          & io_issue_alu_bits_1_rob_idx == io_issue_alu_bits_3_rob_idx) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide RS must not issue one instruction to two ALUs\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & io_issue_alu_fire_2 & io_issue_alu_fire_3
          & io_issue_alu_bits_2_rob_idx == io_issue_alu_bits_3_rob_idx) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide RS must not issue one instruction to two ALUs\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_issue_lsu1_fire | ~io_issue_lsu1_bits_lsu_mem_write)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: the secondary LSU issue path is load-only\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset
          & ~(~(io_issue_lsu_fire & io_issue_lsu1_fire)
              | io_issue_lsu_idx != io_issue_lsu1_idx)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: the two LSU ports must not issue the same RS entry\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset
          & ~(~io_issue_store_addr_fire | io_issue_store_addr_bits_lsu_mem_write
              & io_issue_store_addr_bits_src1_ready
              & ~io_issue_store_addr_bits_src2_ready)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: Store address issue must be an address-ready/data-wait Store\n");
        if (`STOP_COND_)
          $fatal;
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
endmodule

