# 阶段 13f：IPC 1.5/1.6 验收与严格回归

## 学习导航
- **理论目标**：把双发极限优化冻结成可重复参考点，并为四发重构提供可信的功能、性能和宽度基线。
- **最小实现**：完整 compile/unit/cpu-tests+difftest，一次 full microbench 加同二进制 repeat，输出 Stage12f/Stage13 全部 A/B 账本。
- **当前参考核**：Stage13f 已通过最终回归，两次 IPC 均为 `1.5134`（`345032` cycles / `522164` commits）；硬目标通过，冲刺目标 `1.60` 未达。
- **后续扩展**：Stage14 先证明 WIDTH=2 参数化等价，再打开四槽前后端；不得把 Stage13 未完成工作改名后移。
- **验收方式**：IPC `>=1.50`、repeat delta `<=0.005`、两轮 PASS+GOOD TRAP、7/7 cpu-tests、全部 OoOUnitTest、零 FTQ stale/内存序错误/新增 warning。

---

## 1. 最终脚本合同

```bash
cd $NPC_HOME
scripts/stage13_final.sh
```

脚本必须：

1. `mychisel.compile` 和完整 `OoOUnitTest`。
2. warning-free Chisel generation。
3. `dummy/add/add-longlong/bit/load-store/shift/string` 全部 difftest。
4. full microbench 与同一 simulator、同一 binary repeat。
5. 执行 IPC、正确性、结构使用和重复性门槛。

## 2. 性能与结构门槛

```text
Hard IPC target            >= 1.5000
Stretch target             >= 1.6000
repeat IPC delta           <= 0.005
average fetch width        >= 1.82
commit slot1 utilization   >= 75%
DCache hit rate            >= 97%
FTQ stale recover          = 0
memory-order violation     = 0
CDB conflict               <= 500
RS fresh issue             > 0
```

最终回归达到 IPC `1.5134`，所以阶段目标应表述为“1.5 硬门槛通过，1.6 冲刺目标未达”。双发核不因没有达到 1.6 而伪造数字，也不把剩余提升偷放到下一阶段；Stage13 已完成其明确合同，Stage14 是宽度重构。

## 3. 最终验收结果

| 指标 | 结果 |
|------|------|
| IPC / cycles / commits | `1.5134 / 345032 / 522164` |
| avg fetch / slot1 util | `1.8650 / 76.83%` |
| FQ full / empty | `1032 / 21550` |
| BP flush / direction / target / JALR | `12435 / 11586 / 439 / 410` |
| CDB conflict / RS fresh issue | `128 / 241331` |
| head wait | `10880` |
| DCache hit rate | `98.02%` |
| stale FTQ / memory violation | `0 / 0` |

```text
result root   build/stage9_regress/stage13_final_20260828_171325
compile       PASS
OoOUnitTest   97/97 PASS
chisel-gen    PASS, no new warning/combinational loop
cpu-tests     7/7 PASS with difftest
run1/run2     IPC 1.5134, cycles 345032, commits 522164
repeat delta  0.0000
```

## 4. 提交边界

阶段 13 只有在全部门槛通过、讲义与源码/日志一致后才能提交：

```text
完成乱序讲义stage13
```

提交不包含 VM 外层无关子模块修改。

## 5. 验收清单（学习者自勾）

- [ ] 完整单测和 7/7 difftest 通过
- [ ] 两次同二进制结果可重复
- [ ] IPC 达到硬门槛且正确性计数为零
- [ ] A/B CSV 包含保留和拒绝点
- [ ] 能从 Stage13f 基线解释为何进入四发

下一章：[15_阶段14_真乱序四发与IPC2.2.md](15_阶段14_真乱序四发与IPC2.2.md)。
