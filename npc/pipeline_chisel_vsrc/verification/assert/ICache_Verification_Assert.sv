// Generated by CIRCT firtool-1.114.1

// Users can define 'STOP_COND' to add an extra gate to stop conditions.
`ifndef STOP_COND_
  `ifdef STOP_COND
    `define STOP_COND_ (`STOP_COND)
  `else  // STOP_COND
    `define STOP_COND_ 1
  `endif // STOP_COND
`endif // not def STOP_COND_

// Users can define 'ASSERT_VERBOSE_COND' to add an extra gate to assert error printing.
`ifndef ASSERT_VERBOSE_COND_
  `ifdef ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ (`ASSERT_VERBOSE_COND)
  `else  // ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ 1
  `endif // ASSERT_VERBOSE_COND
`endif // not def ASSERT_VERBOSE_COND_
module ICache_Verification_Assert(
  input       reset,
  input [7:0] ar_stall_cnt,
              r_stall_cnt,
  input [1:0] io_out_arburst,
  input       _GEN,
  input [2:0] io_out_arsize,
  input [1:0] _GEN_0,
  input       _GEN_1,
              clock,
              _GEN_2,
              io_out_arvalid
);

  `ifndef SYNTHESIS
    wire _GEN_3 = io_out_arvalid & ~reset;
    always @(posedge clock) begin
      if (_GEN_1 & ~reset & ar_stall_cnt > 8'hC7) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] ICache.OUT: arvalid stuck waiting for arready!\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (_GEN_2 & ~reset & r_stall_cnt > 8'hC7) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] ICache.OUT: rvalid stuck after AR handshake!\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (_GEN_3 & ~(io_out_arburst == {1'h0, _GEN} | io_out_arburst == 2'h0)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] ICache.OUT: arburst invalid (FIXED=0, INCR=1)\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (_GEN_3 & io_out_arsize != {1'h0, _GEN_0}) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] ICache.OUT: arsize must be 2 (4-byte) for inst fetch\n");
        if (`STOP_COND_)
          $fatal;
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
endmodule

