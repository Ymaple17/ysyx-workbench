// Generated by CIRCT firtool-1.114.1

// Users can define 'STOP_COND' to add an extra gate to stop conditions.
`ifndef STOP_COND_
  `ifdef STOP_COND
    `define STOP_COND_ (`STOP_COND)
  `else  // STOP_COND
    `define STOP_COND_ 1
  `endif // STOP_COND
`endif // not def STOP_COND_

// Users can define 'ASSERT_VERBOSE_COND' to add an extra gate to assert error printing.
`ifndef ASSERT_VERBOSE_COND_
  `ifdef ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ (`ASSERT_VERBOSE_COND)
  `else  // ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ 1
  `endif // ASSERT_VERBOSE_COND
`endif // not def ASSERT_VERBOSE_COND_
module WritebackArbiter_Verification_Assert(
  input       reset,
  input [1:0] _GEN,
              _GEN_0,
              _GEN_1,
              _GEN_2,
              _GEN_3,
              _GEN_4,
              _GEN_5,
              _GEN_6,
              _GEN_7,
              _GEN_8,
              _GEN_9,
              _GEN_10,
              _GEN_11,
              _GEN_12,
              _GEN_13,
              _GEN_14,
  input       io_out_0_valid,
              io_out_1_valid,
  input [4:0] io_out_0_bits_rob_idx,
              io_robHead,
              io_out_1_bits_rob_idx,
  input       io_out_2_valid,
  input [4:0] io_out_2_bits_rob_idx,
  input       io_out_3_valid,
  input [4:0] io_out_3_bits_rob_idx,
  input       clock
);

  `ifndef SYNTHESIS
    wire [7:0] _GEN_15 = {_GEN_5, _GEN_6, _GEN_3, _GEN_4};
    wire [7:0] _GEN_16 = {_GEN_1, _GEN_2, _GEN, _GEN_0};
    wire [7:0] _GEN_17 = {_GEN_9, _GEN_10, _GEN_7, _GEN_8};
    wire [7:0] _GEN_18 = {_GEN_13, _GEN_14, _GEN_11, _GEN_12};
    wire [4:0] _GEN_19 = io_out_1_bits_rob_idx - io_robHead;
    wire [4:0] _GEN_20 = io_out_2_bits_rob_idx - io_robHead;
    always @(posedge clock) begin
      if (~reset & (|(_GEN_16 & _GEN_15))) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: writeback ports must not grant the same result\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & (|(_GEN_16 & _GEN_17))) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: writeback ports must not grant the same result\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & (|(_GEN_16 & _GEN_18))) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: writeback ports must not grant the same result\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & (|(_GEN_15 & _GEN_17))) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: writeback ports must not grant the same result\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & (|(_GEN_15 & _GEN_18))) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: writeback ports must not grant the same result\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & (|(_GEN_17 & _GEN_18))) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: writeback ports must not grant the same result\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & io_out_0_valid & io_out_1_valid & io_out_0_bits_rob_idx
          - io_robHead > _GEN_19) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: writeback grants must be ordered by ROB age\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & io_out_1_valid & io_out_2_valid & _GEN_19 > _GEN_20) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: writeback grants must be ordered by ROB age\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & io_out_2_valid & io_out_3_valid & _GEN_20 > io_out_3_bits_rob_idx
          - io_robHead) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: writeback grants must be ordered by ROB age\n");
        if (`STOP_COND_)
          $fatal;
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
endmodule

