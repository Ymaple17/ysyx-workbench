// Generated by CIRCT firtool-1.114.1

// Users can define 'STOP_COND' to add an extra gate to stop conditions.
`ifndef STOP_COND_
  `ifdef STOP_COND
    `define STOP_COND_ (`STOP_COND)
  `else  // STOP_COND
    `define STOP_COND_ 1
  `endif // STOP_COND
`endif // not def STOP_COND_

// Users can define 'ASSERT_VERBOSE_COND' to add an extra gate to assert error printing.
`ifndef ASSERT_VERBOSE_COND_
  `ifdef ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ (`ASSERT_VERBOSE_COND)
  `else  // ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ 1
  `endif // ASSERT_VERBOSE_COND
`endif // not def ASSERT_VERBOSE_COND_
module StoreAddressSidecar_Verification_Assert(
  input reset,
        io_issue_bits_lsu_mem_valid,
        io_issue_bits_lsu_mem_write,
        io_issue_valid,
        io_issue_bits_src2_ready,
        io_issue_bits_src1_ready,
        clock
);

  `ifndef SYNTHESIS
    wire _GEN = ~reset & io_issue_valid;
    always @(posedge clock) begin
      if (_GEN & ~(io_issue_bits_lsu_mem_valid & io_issue_bits_lsu_mem_write)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: StoreAddressSidecar accepts only Store uops\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (_GEN & ~(io_issue_bits_src1_ready & ~io_issue_bits_src2_ready)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: StoreAddressSidecar is reserved for address-ready/data-wait Stores\n");
        if (`STOP_COND_)
          $fatal;
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
endmodule
