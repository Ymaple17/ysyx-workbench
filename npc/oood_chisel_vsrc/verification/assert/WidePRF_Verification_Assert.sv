// Generated by CIRCT firtool-1.114.1

// Users can define 'STOP_COND' to add an extra gate to stop conditions.
`ifndef STOP_COND_
  `ifdef STOP_COND
    `define STOP_COND_ (`STOP_COND)
  `else  // STOP_COND
    `define STOP_COND_ 1
  `endif // STOP_COND
`endif // not def STOP_COND_

// Users can define 'ASSERT_VERBOSE_COND' to add an extra gate to assert error printing.
`ifndef ASSERT_VERBOSE_COND_
  `ifdef ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ (`ASSERT_VERBOSE_COND)
  `else  // ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ 1
  `endif // ASSERT_VERBOSE_COND
`endif // not def ASSERT_VERBOSE_COND_
module WidePRF_Verification_Assert(
  input       reset,
              io_wen_0,
              io_wen_1,
  input [5:0] io_waddr_0,
              io_waddr_1,
  input       _GEN,
              io_wen_2,
  input [5:0] io_waddr_2,
  input       io_wen_3,
  input [5:0] io_waddr_3,
  input       _GEN_0,
              _GEN_1,
              clock
);

  `ifndef SYNTHESIS
    always @(posedge clock) begin
      if (~reset & io_wen_0 & io_wen_1 & _GEN & io_waddr_0 == io_waddr_1) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide PRF writes must have unique destinations\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & io_wen_0 & io_wen_2 & _GEN & io_waddr_0 == io_waddr_2) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide PRF writes must have unique destinations\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & io_wen_0 & io_wen_3 & _GEN & io_waddr_0 == io_waddr_3) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide PRF writes must have unique destinations\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & io_wen_1 & io_wen_2 & _GEN_0 & io_waddr_1 == io_waddr_2) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide PRF writes must have unique destinations\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & io_wen_1 & io_wen_3 & _GEN_0 & io_waddr_1 == io_waddr_3) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide PRF writes must have unique destinations\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & io_wen_2 & io_wen_3 & _GEN_1 & io_waddr_2 == io_waddr_3) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide PRF writes must have unique destinations\n");
        if (`STOP_COND_)
          $fatal;
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
endmodule

