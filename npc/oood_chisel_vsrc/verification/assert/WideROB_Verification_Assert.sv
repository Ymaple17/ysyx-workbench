// Generated by CIRCT firtool-1.114.1

// Users can define 'STOP_COND' to add an extra gate to stop conditions.
`ifndef STOP_COND_
  `ifdef STOP_COND
    `define STOP_COND_ (`STOP_COND)
  `else  // STOP_COND
    `define STOP_COND_ 1
  `endif // STOP_COND
`endif // not def STOP_COND_

// Users can define 'ASSERT_VERBOSE_COND' to add an extra gate to assert error printing.
`ifndef ASSERT_VERBOSE_COND_
  `ifdef ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ (`ASSERT_VERBOSE_COND)
  `else  // ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ 1
  `endif // ASSERT_VERBOSE_COND
`endif // not def ASSERT_VERBOSE_COND_
module WideROB_Verification_Assert(
  input       reset,
              io_enq_fire_1,
              io_enq_fire_0,
              io_commit_fire_1,
              io_commit_fire_0,
              io_enq_fire_2,
              io_commit_fire_2,
              io_enq_fire_3,
              io_commit_fire_3,
              io_commit_valid_0,
              io_commit_valid_1,
              io_commit_valid_2,
              io_commit_valid_3,
              io_flush,
              io_flush_all,
              entries_2_valid,
              entries_3_valid,
              entries_0_valid,
              entries_1_valid,
              entries_6_valid,
              entries_7_valid,
              entries_4_valid,
              entries_5_valid,
              entries_10_valid,
              entries_11_valid,
              entries_8_valid,
              entries_9_valid,
              entries_14_valid,
              entries_15_valid,
              entries_12_valid,
              entries_13_valid,
              entries_18_valid,
              entries_19_valid,
              entries_16_valid,
              entries_17_valid,
              entries_22_valid,
              entries_23_valid,
              entries_20_valid,
              entries_21_valid,
              entries_26_valid,
              entries_27_valid,
              entries_24_valid,
              entries_25_valid,
              entries_30_valid,
              entries_31_valid,
              entries_28_valid,
              entries_29_valid,
  input [5:0] count,
  input       clock
);

  `ifndef SYNTHESIS
    always @(posedge clock) begin
      if (~reset & ~(~io_enq_fire_1 | io_enq_fire_0)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide ROB enqueue must be a valid prefix\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_commit_fire_1 | io_commit_fire_0)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide ROB commit must be a valid prefix\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_enq_fire_2 | io_enq_fire_1)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide ROB enqueue must be a valid prefix\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_commit_fire_2 | io_commit_fire_1)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide ROB commit must be a valid prefix\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_enq_fire_3 | io_enq_fire_2)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide ROB enqueue must be a valid prefix\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_commit_fire_3 | io_commit_fire_2)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide ROB commit must be a valid prefix\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_commit_fire_0 | io_commit_valid_0)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide ROB can only commit completed entries\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_commit_fire_1 | io_commit_valid_1)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide ROB can only commit completed entries\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_commit_fire_2 | io_commit_valid_2)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide ROB can only commit completed entries\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_commit_fire_3 | io_commit_valid_3)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide ROB can only commit completed entries\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset
          & ~(~(io_flush | io_flush_all)
              | {io_enq_fire_3, io_enq_fire_2, io_enq_fire_1, io_enq_fire_0} == 4'h0
              & {io_commit_fire_3,
                 io_commit_fire_2,
                 io_commit_fire_1,
                 io_commit_fire_0} == 4'h0)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: redirect cycles must not enqueue or commit\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset
          & {1'h0,
             {1'h0,
              {1'h0,
               {1'h0, {1'h0, entries_0_valid} + {1'h0, entries_1_valid}}
                 + {1'h0, {1'h0, entries_2_valid} + {1'h0, entries_3_valid}}}
                + {1'h0,
                   {1'h0, {1'h0, entries_4_valid} + {1'h0, entries_5_valid}}
                     + {1'h0, {1'h0, entries_6_valid} + {1'h0, entries_7_valid}}}}
               + {1'h0,
                  {1'h0,
                   {1'h0, {1'h0, entries_8_valid} + {1'h0, entries_9_valid}}
                     + {1'h0, {1'h0, entries_10_valid} + {1'h0, entries_11_valid}}}
                    + {1'h0,
                       {1'h0, {1'h0, entries_12_valid} + {1'h0, entries_13_valid}}
                         + {1'h0, {1'h0, entries_14_valid} + {1'h0, entries_15_valid}}}}}
          + {1'h0,
             {1'h0,
              {1'h0,
               {1'h0, {1'h0, entries_16_valid} + {1'h0, entries_17_valid}}
                 + {1'h0, {1'h0, entries_18_valid} + {1'h0, entries_19_valid}}}
                + {1'h0,
                   {1'h0, {1'h0, entries_20_valid} + {1'h0, entries_21_valid}}
                     + {1'h0, {1'h0, entries_22_valid} + {1'h0, entries_23_valid}}}}
               + {1'h0,
                  {1'h0,
                   {1'h0, {1'h0, entries_24_valid} + {1'h0, entries_25_valid}}
                     + {1'h0, {1'h0, entries_26_valid} + {1'h0, entries_27_valid}}}
                    + {1'h0,
                       {1'h0, {1'h0, entries_28_valid} + {1'h0, entries_29_valid}}
                         + {1'h0,
                            {1'h0, entries_30_valid}
                              + {1'h0, entries_31_valid}}}}} != count) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: wide ROB valid entries must match count\n");
        if (`STOP_COND_)
          $fatal;
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
endmodule

