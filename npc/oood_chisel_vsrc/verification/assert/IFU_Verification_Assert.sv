// Generated by CIRCT firtool-1.114.1

// Users can define 'STOP_COND' to add an extra gate to stop conditions.
`ifndef STOP_COND_
  `ifdef STOP_COND
    `define STOP_COND_ (`STOP_COND)
  `else  // STOP_COND
    `define STOP_COND_ 1
  `endif // STOP_COND
`endif // not def STOP_COND_

// Users can define 'ASSERT_VERBOSE_COND' to add an extra gate to assert error printing.
`ifndef ASSERT_VERBOSE_COND_
  `ifdef ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ (`ASSERT_VERBOSE_COND)
  `else  // ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ 1
  `endif // ASSERT_VERBOSE_COND
`endif // not def ASSERT_VERBOSE_COND_
module IFU_Verification_Assert(
  input       reset,
              ftq_io_alloc_ready,
              ftq_io_alloc_valid,
              _GEN,
              _GEN_0,
              ftq_io_recoverValid,
  input [1:0] _GEN_1,
              _GEN_2,
  input       clock
);

  `ifndef SYNTHESIS
    always @(posedge clock) begin
      if (~reset & _GEN != (ftq_io_alloc_ready & ftq_io_alloc_valid)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: FetchBuffer and FTQ must allocate the same fetch block\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(_GEN_0 | ftq_io_recoverValid)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: branch recovery must find its FTQ generation\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & {_GEN_0, _GEN_1, _GEN_2} == 5'h0) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: branch recovery PC must identify a live FTQ lane\n");
        if (`STOP_COND_)
          $fatal;
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
endmodule

