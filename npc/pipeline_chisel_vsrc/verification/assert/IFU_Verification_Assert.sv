// Generated by CIRCT firtool-1.114.1

// Users can define 'STOP_COND' to add an extra gate to stop conditions.
`ifndef STOP_COND_
  `ifdef STOP_COND
    `define STOP_COND_ (`STOP_COND)
  `else  // STOP_COND
    `define STOP_COND_ 1
  `endif // STOP_COND
`endif // not def STOP_COND_

// Users can define 'ASSERT_VERBOSE_COND' to add an extra gate to assert error printing.
`ifndef ASSERT_VERBOSE_COND_
  `ifdef ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ (`ASSERT_VERBOSE_COND)
  `else  // ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ 1
  `endif // ASSERT_VERBOSE_COND
`endif // not def ASSERT_VERBOSE_COND_
module IFU_Verification_Assert(
  input        reset,
  input [7:0]  ar_stall_cnt,
               r_stall_cnt,
  input        ar_addr_latched,
               io_imem_arvalid,
  input [31:0] io_imem_araddr,
               ar_addr_stable,
  input        _GEN,
               clock,
               _GEN_0
);

  `ifndef SYNTHESIS
    always @(posedge clock) begin
      if (_GEN & ~reset & ar_stall_cnt > 8'hC7) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] IFU.IMEM: arvalid stuck waiting for arready!\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (_GEN_0 & ~reset & r_stall_cnt > 8'hC7) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] IFU.IMEM: rvalid stuck after AR handshake!\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (ar_addr_latched & io_imem_arvalid & ~reset
          & io_imem_araddr != ar_addr_stable) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: [AXI-MONITOR] IFU.IMEM: araddr changed while arvalid asserted!\n");
        if (`STOP_COND_)
          $fatal;
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
endmodule

