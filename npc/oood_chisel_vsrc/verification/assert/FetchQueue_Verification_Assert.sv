// Generated by CIRCT firtool-1.114.1

// Users can define 'STOP_COND' to add an extra gate to stop conditions.
`ifndef STOP_COND_
  `ifdef STOP_COND
    `define STOP_COND_ (`STOP_COND)
  `else  // STOP_COND
    `define STOP_COND_ 1
  `endif // STOP_COND
`endif // not def STOP_COND_

// Users can define 'ASSERT_VERBOSE_COND' to add an extra gate to assert error printing.
`ifndef ASSERT_VERBOSE_COND_
  `ifdef ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ (`ASSERT_VERBOSE_COND)
  `else  // ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ 1
  `endif // ASSERT_VERBOSE_COND
`endif // not def ASSERT_VERBOSE_COND_
module FetchQueue_Verification_Assert(
  input reset,
        io_enq_bits_valid_1,
        io_enq_bits_valid_0,
        io_enq_bits_valid_2,
        io_enq_bits_valid_3,
        clock
);

  `ifndef SYNTHESIS
    always @(posedge clock) begin
      if (~reset & ~(~io_enq_bits_valid_1 | io_enq_bits_valid_0)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: fetch queue enqueue must be a valid prefix\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_enq_bits_valid_2 | io_enq_bits_valid_1)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: fetch queue enqueue must be a valid prefix\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~reset & ~(~io_enq_bits_valid_3 | io_enq_bits_valid_2)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: fetch queue enqueue must be a valid prefix\n");
        if (`STOP_COND_)
          $fatal;
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
endmodule

